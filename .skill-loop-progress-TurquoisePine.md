# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/frankentorch/crates/ft-api/src/lib.rs
# Total Passes: 5
# Started: 2026-06-03T05:34:00Z

## Status: COMPLETE - Pass 5 of 5

## Coordination
- Agent: TurquoisePine
- Canonical progress file `.skill-loop-progress.md` is reserved by BlackThrush until 2026-06-03T06:39:18Z.
- Tracker file `.beads/issues.jsonl` is reserved by SilentSnow until 2026-06-03T06:38:43Z.
- Active local reservation: `crates/ft-api/src/lib.rs`, `crates/ft-api/benches/ops_bench.rs`, `artifacts/perf/ft_api_sinusoidal_*`.

## Missions
1. Target and baseline: establish a measured, disjoint profile-backed target.
2. One lever: precompute sinusoidal positional encoding denominators once per column.
3. Proof: verify ordering, tie-breaking, floating-point bit identity, RNG, and golden output sha256.
4. Correctness gates: run focused crate-scoped checks/tests via rch.
5. Re-benchmark and closeout: re-run Criterion, keep only if Score >= 2.0, commit with numbers, then claim/close bead when tracker reservation clears.

## Completed Passes

### Pass 1 - Target and baseline - 2026-06-03T05:34:00Z
- Files changed: `.skill-loop-progress-TurquoisePine.md`
- Baseline command: `rch exec -- cargo bench -p ft-api --bench ops_bench -- sinusoidal_pe/4096x1024`
- Baseline result: `[34.676 ms 37.418 ms 40.664 ms]`, throughput `[103.15 Melem/s 112.09 Melem/s 120.96 Melem/s]`
- Prior related bead: `frankentorch-kgs4.22` parallelized row fill and proved bit-exact serial parity.
- Remaining hotspot signature: one `powf` denominator recomputation per output element despite identical denominator for each column.
- Opportunity score: Impact 4 * Confidence 5 / Effort 2 = 10.0.
- Verdict: PRODUCTIVE

### Pass 2 - One lever implementation - 2026-06-03T05:38:00Z
- Files changed: `crates/ft-api/src/lib.rs`
- Changes: precomputed the sinusoidal positional encoding denominator once per column, then kept each element's angle as `pos as f64 / denominators[i]`.
- Subagent: Dirac (`019e8bfa-6874-7a23-b8c4-3784f1c7d2ac`) supplied the exact patch recommendation and did not edit through TurquoisePine's reservation.
- Isomorphism intent: row ordering, linear write position, RNG, and per-element division are unchanged; repeated `powf` calls are replaced by one identical denominator computation per column.
- Verdict: PRODUCTIVE

### Pass 3 - Proof - 2026-06-03T05:40:00Z
- Files changed: none
- Golden output: serial 128x128 `to_bits()` stream sha256 `172c202a726f7bc9ea9696edb21151e0812296d8c1ba14e0c96f0e8693c07264`.
- Behavior proof: `rch exec -- cargo test -p ft-api sinusoidal_position_encoding_parallel_match_serial_bit_exact` passed; 1 test passed, 1764 filtered out.
- Ordering preserved: yes, rows and linear write positions are unchanged.
- Tie-breaking unchanged: N/A.
- Floating-point: bit-exact against the old serial formula by `to_bits()`.
- RNG seeds: N/A.
- Verdict: PRODUCTIVE

### Pass 4 - Correctness gates - 2026-06-03T05:45:00Z
- Files changed: none
- Passed: `rch exec -- cargo check -p ft-api --all-targets`.
- Passed: `rch exec -- cargo test -p ft-api sinusoidal_position_encoding_parallel_match_serial_bit_exact`.
- Blocked by existing debt: `rch exec -- cargo clippy -p ft-api --all-targets -- -D warnings` fails on broad pre-existing ft-api lints unrelated to the changed lines.
- Blocked by existing drift: `rch exec -- cargo fmt -p ft-api --check` reports formatting diffs across unrelated ft-api bench and source regions; the changed hunk is already formatted.
- Blocked by existing inventory: `ubs crates/ft-api/src/lib.rs` reports 325 critical, 16785 warning, and 1950 info findings across the whole 80k-line file; no finding is specific to the changed denominator precompute hunk.
- Verdict: PRODUCTIVE with unrelated gate debt documented.

### Pass 5 - Re-benchmark and keep/reject - 2026-06-03T05:50:00Z
- Files changed: none
- Re-bench command: `rch exec -- cargo bench -p ft-api --bench ops_bench -- sinusoidal_pe/4096x1024`.
- Same worker as baseline: `vmi1153651`.
- Before: `[34.676 ms 37.418 ms 40.664 ms]`, throughput `[103.15 Melem/s 112.09 Melem/s 120.96 Melem/s]`.
- After: `[33.819 ms 36.009 ms 38.358 ms]`, throughput `[109.35 Melem/s 116.48 Melem/s 124.02 Melem/s]`.
- Integrated improvement: mean 37.418 ms -> 36.009 ms, about 3.8%.
- Lever microbench: remote `hyperfine --warmup 3 --runs 10` old serial loop `128.8 ms +/- 6.1 ms`, denominator-precompute loop `58.3 ms +/- 2.2 ms`, 2.21x faster.
- Score: Impact 2 * Confidence 4 / Effort 1 = 8.0.
- Landed code commit: `0b9972f2` advanced HEAD during TurquoisePine's pre-commit hook and included this sinusoidal hunk plus this progress file alongside another agent's `frankentorch-m6c4` linalg work.
- Closeout action: add a follow-up TurquoisePine commit with the required before/after numbers in the commit message rather than amending another agent's commit.
- Verdict: PRODUCTIVE; keep.

---

# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/frankentorch/crates/ft-device/src/lib.rs
# Total Passes: 5
# Started: 2026-06-03T06:08:00Z

## Status: IN PROGRESS - Pass 3 of 5

## Coordination
- Agent: TurquoisePine
- `br ready --json` is empty after a brief wait.
- Active perf/no-gaps beads are claimed by RusticKite (`frankentorch-kgs4`, `frankentorch-kgs4.4`) and SilentSnow (`frankentorch-x1kx`).
- Active local reservation: `crates/ft-device/src/lib.rs`, `crates/ft-device/benches/device_bench.rs`, `.skill-loop-progress-TurquoisePine.md`.
- Tracker file `.beads/issues.jsonl` is still avoided because it is dirty/reserved by another agent.

## Missions
1. Target and baseline: establish Criterion/hyperfine evidence for the `device_guard/mismatch_display_65536` formatter hot path.
2. One lever: replace generic `Debug` formatting in `DeviceError::Display` with exact literal device-name emission if the baseline/score gate clears.
3. Proof: preserve exact Display bytes, ordering, tie-breaking, floating-point, RNG, and golden output sha256.
4. Correctness gates: run focused crate-scoped tests/checks via rch and UBS on the touched file.
5. Re-benchmark and closeout: re-run Criterion/hyperfine, keep only if Score >= 2.0, commit with numbers, and re-check perf beads.

## Completed Passes

### Pass 1 - Target and baseline - 2026-06-03T06:15:00Z
- Files changed: `.skill-loop-progress-TurquoisePine.md`
- Baseline command: `rch exec -- cargo bench -p ft-device --bench device_bench -- device_guard/mismatch_display_65536 --warm-up-time 1 --measurement-time 5 --sample-size 20`.
- Baseline result: `[1.8790 ms 1.9159 ms 1.9628 ms]` for 65,536 mismatch displays. RCH reported local fallback because no admissible workers were available.
- Alien primitive: constants-kill-you / shape-specialized hot path from the canonical graveyard guidance, using profile-first validation instead of assuming the constant-factor rewrite wins.
- Candidate score before implementation: Impact 2 * Confidence 4 / Effort 1 = 8.0.
- Verdict: PRODUCTIVE baseline.

### Pass 2 - Literal formatter lever - 2026-06-03T06:27:00Z
- Files changed temporarily: `crates/ft-device/src/lib.rs`.
- Change attempted: replaced `expected {expected:?}, got {actual:?}` with exact `Cpu`/`Cuda` literal-name emission and added a focused golden-string test.
- Golden output: four device-pair mismatch strings sha256 `699b917aaf5896a28a0ed10ea2d9584e2981c4fb12f57b0d38c2d1b78fc4a760`; `sha256sum -c` passed.
- Correctness checks during attempt: `rch exec -- cargo test -p ft-device mismatch_display_preserves_debug_name_bytes -- --nocapture` passed; `rch exec -- cargo check -p ft-device --all-targets` passed; `rch exec -- cargo clippy -p ft-device --all-targets -- -D warnings` passed.
- UBS during attempt: no critical issues; after rewriting the test away from a loop, residual warnings were existing/test-only assert/expect inventory.
- Re-benchmark: Criterion after run landed on a different remote worker and was not comparable. Controlled side-by-side hyperfine on one host: old `Debug` formatter `29.1 ms +/- 1.3 ms`, literal helper `32.6 ms +/- 1.0 ms`; old ran `1.12x` faster.
- Verdict: REJECTED; source hunk manually reverted, no commit.

---

# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/frankentorch/crates/ft-data/src/lib.rs
# Total Passes: 5
# Started: 2026-06-03T06:31:00Z

## Status: COMPLETE - Pass 5 of 5

## Coordination
- Agent: TurquoisePine
- `br ready --json` remained empty, so this fallback target was selected from profiler/criterion-visible sampler evidence.
- Active perf/no-gaps beads stayed claimed by RusticKite (`frankentorch-kgs4`, `frankentorch-kgs4.4`) and SilentSnow (`frankentorch-x1kx`).
- Active local reservation: `crates/ft-data/src/lib.rs`, `crates/ft-data/benches/sampler_bench.rs`, `.skill-loop-progress-TurquoisePine.md`.
- Tracker file `.beads/issues.jsonl` is still avoided because it is dirty/reserved by another agent.

## Completed Passes

### Pass 1 - Target and baseline - 2026-06-03T06:32:00Z
- Baseline command: `rch exec -- cargo bench -p ft-data --bench sampler_bench -- sampler/weighted_two_positive_2x1m --warm-up-time 1 --measurement-time 5 --sample-size 20`.
- Baseline worker: `vmi1156319`.
- Baseline result: `[10.305 ms 10.783 ms 11.291 ms]`.
- Profile-backed target: two-positive-weight weighted sampling spends the hot loop normalizing thresholds and doing a binary search even though the domain is exactly two buckets.
- Candidate score before implementation: Impact 4 * Confidence 4 / Effort 1 = 16.0.

### Pass 2 - One lever - 2026-06-03T06:36:00Z
- Files changed: `crates/ft-data/src/lib.rs`.
- Change: added a length-2 weighted sampler branch that computes the first cumulative threshold once and maps each deterministic RNG draw with `u <= first_threshold`.
- Isomorphism intent: validation order and error behavior stay before the branch; RNG seed, draw count, output order, and the binary-search equality/tie rule are preserved for two weights.

### Pass 3 - Proof - 2026-06-03T06:38:00Z
- Golden output: `WeightedRandomSampler::new(vec![1.0, 3.0], 32).with_seed(0x5151_0002).indices()` sha256 `df46947b40c47d93a48b572651b722a6cf1a490ec5e93c5d90983c000291a198`.
- Golden command: `sha256sum -c` passed for the exact 32-index output sequence.
- Behavior proof: `rch exec -- cargo test -p ft-data weighted_random_sampler -- --nocapture` passed on `vmi1293453`; 13 tests passed, including the new two-weight order preservation test.
- Ordering preserved: yes, result pushes one sample per RNG draw in the same sequence.
- Tie-breaking preserved: yes, `u <= first_threshold` matches `binary_search_by(...).unwrap_or_else(|i| i)` for exact threshold equality.
- Floating-point preserved: yes, threshold is still `first_weight / total`; no reordered accumulation occurs before the branch.
- RNG seeds preserved: yes, `SimpleRng::new(self.seed)` and `(rng.next_u64() >> 11) as f64 / (1u64 << 53) as f64` are unchanged.

### Pass 4 - Correctness gates - 2026-06-03T06:44:00Z
- Passed: `cargo fmt -p ft-data --check`.
- Passed: `rch exec -- cargo check -p ft-data --all-targets`.
- Passed: `rch exec -- cargo clippy -p ft-data --all-targets --no-deps -- -D warnings`.
- Blocked by dependency lint debt: `rch exec -- cargo clippy -p ft-data --all-targets -- -D warnings` fails in `crates/ft-api/src/lib.rs` with 61 existing clippy errors before ft-data-specific linting.
- UBS: `ubs crates/ft-data/src/lib.rs` still exits nonzero on existing inventory, including two false-positive secret-compare findings in shape/test comparisons; no new direct-indexing warning remains in the two-weight fast path.

### Pass 5 - Re-benchmark and keep/reject - 2026-06-03T06:47:00Z
- Re-bench command: `rch exec -- cargo bench -p ft-data --bench sampler_bench -- sampler/weighted_two_positive_2x1m --warm-up-time 1 --measurement-time 5 --sample-size 20`.
- After worker: `vmi1149989`.
- Before: `[10.305 ms 10.783 ms 11.291 ms]`.
- After: `[2.7069 ms 2.7613 ms 2.8211 ms]`.
- Integrated improvement: mean 10.783 ms -> 2.7613 ms, about 74.4% faster elapsed time and 3.90x throughput.
- Score: Impact 4 * Confidence 4 / Effort 1 = 16.0.
- Verdict: PRODUCTIVE; keep.

---

# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/frankentorch/crates/ft-nn/src/lib.rs
# Total Passes: 5
# Started: 2026-06-03T07:05:00Z

## Status: COMPLETE - Pass 5 of 5

## Coordination
- Agent: TurquoisePine
- `br ready --json` remained empty; all visible perf/no-gaps beads were already in progress and claimed by other agents.
- Active fallback target came from profiler/criterion-visible `MultiheadAttention::forward_qkv` evidence supplied by Goodall and confirmed by `nn_bench`.
- Active local reservation: `crates/ft-nn/src/lib.rs`, `crates/ft-nn/benches/nn_bench.rs`, `crates/ft-autograd/src/lib.rs`, `.skill-loop-progress-TurquoisePine.md`.

## Completed Passes

### Pass 1 - Target and baseline - 2026-06-03T07:08:00Z
- Baseline command: `rch exec -- cargo bench -p ft-nn --bench nn_bench -- multihead_attention/forward_8x64x128_h8 --warm-up-time 1 --measurement-time 5 --sample-size 20`.
- Baseline worker/result: `vmi1167313`, `[46.372 ms 51.098 ms 57.384 ms]`.
- Same-worker control baseline for the pre-change path: `vmi1227854`, `[17.389 ms 17.987 ms 18.881 ms]`.
- Profile-backed target: MHA query scaling allocated a full `[N * heads, S_q, head_dim]` tensor before multiplying every query value by a scalar.
- Candidate score before implementation: Impact 2 * Confidence 4 / Effort 2 = 4.0.

### Pass 2 - One lever - 2026-06-03T07:18:00Z
- Files changed: `crates/ft-autograd/src/lib.rs`, `crates/ft-nn/src/lib.rs`.
- Change: added a differentiable `TensorTape::mul_scalar` primitive and replaced the MHA full scale tensor plus `tensor_mul` with direct scalar multiplication.
- One lever only: remove the scale tensor materialization from MHA query scaling while preserving the same autograd gradient for the query input.

### Pass 3 - Behavior proof - 2026-06-03T07:30:00Z
- Golden output: `artifacts/optimization/golden_outputs/ft_nn_mha_batched_heads_pass24.txt` sha256 `d3b78b1fbb9f1cbd157bd420c28f40d678f5e9b75ab27e4a30a23fc47ecc03b5`.
- Golden verification: `sha256sum -c` passed for the MHA fixture, and `rch exec -- cargo test -p ft-nn mha_batched_heads_golden_output_matches_fixture -- --nocapture` passed remotely on `vmi1227854`.
- Autograd equivalence: `rch exec -- cargo test -p ft-autograd tensor_mul_scalar -- --nocapture` passed remotely on `vmi1227854`; direct scalar multiply output/gradient matches a non-grad full-scale RHS tensor, and f32 input promotion matches the old f64 scale tensor path.
- Ordering preserved: MHA still reshapes Q/K/V, scales Q once, then feeds the same `bmm` pipeline.
- Tie-breaking preserved: no sorting, argmax, equality-branch, or selection logic changed.
- Floating-point preserved: each Q element is multiplied by the same f64 `self.scale`; tests compare direct scalar output/gradient to the full scale tensor path.
- RNG preserved: no RNG paths changed.

### Pass 4 - Correctness gates - 2026-06-03T07:42:00Z
- Passed: `rch exec -- cargo check -p ft-nn --all-targets`.
- Passed: `rch exec -- cargo clippy -p ft-nn --all-targets --no-deps -- -D warnings`.
- Passed: `git diff --check -- crates/ft-autograd/src/lib.rs crates/ft-nn/src/lib.rs`.
- UBS: `ubs crates/ft-autograd/src/lib.rs crates/ft-nn/src/lib.rs` exits nonzero on existing inventory, while its built-in fmt, clippy, check, test-build, audit, and deny probes passed.
- Formatting caveat: `cargo fmt -p ft-autograd -p ft-api -p ft-nn --check` is blocked by pre-existing unrelated formatting drift in peer-touched files; the touched diff is whitespace-clean.

### Pass 5 - Re-benchmark and keep/reject - 2026-06-03T07:55:00Z
- Re-bench command: `rch exec -- cargo bench -p ft-nn --bench nn_bench -- multihead_attention/forward_8x64x128_h8 --warm-up-time 1 --measurement-time 5 --sample-size 20`.
- Same-worker before: `vmi1227854`, `[17.389 ms 17.987 ms 18.881 ms]`.
- Same-worker after: `vmi1227854`, `[16.101 ms 16.390 ms 16.793 ms]`.
- Secondary after run: `vmi1293453`, `[15.482 ms 15.858 ms 16.337 ms]`.
- Integrated improvement: mean 17.987 ms -> 16.390 ms, about 8.9% faster elapsed time.
- Score: Impact 2 * Confidence 4 / Effort 2 = 4.0.
- Verdict: PRODUCTIVE; keep.
