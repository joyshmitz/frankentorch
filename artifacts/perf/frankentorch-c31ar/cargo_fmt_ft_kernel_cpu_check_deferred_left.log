Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/benches/linalg_bench.rs:6:
 use criterion::{BatchSize, Criterion, black_box, criterion_group, criterion_main};
 use ft_core::{DType, Device, TensorMeta};
 use ft_kernel_cpu::{
[31m-    cholesky_contiguous_f64, cholesky_solve_contiguous_f64, det_contiguous_f64,
[m[31m-    eig_contiguous_f64, eigh_contiguous_f64,
[m[31m-    eigvals_contiguous_f64, eigvalsh_contiguous_f64, inv_tensor_contiguous_f64,
[m[31m-    lobpcg_contiguous_f64, lu_factor_contiguous_f64, lu_solve_contiguous_f64,
[m[31m-    lu_solve_mixed_refine_contiguous_f64, matrix_exp_contiguous_f64, qr_contiguous_f64,
[m[31m-    svd_contiguous_f64, svd_lowrank_contiguous_f64, svdvals_contiguous_f64,
[m[32m+    cholesky_contiguous_f64, cholesky_solve_contiguous_f64, det_contiguous_f64, eig_contiguous_f64,
[m[32m+    eigh_contiguous_f64, eigvals_contiguous_f64, eigvalsh_contiguous_f64,
[m[32m+    inv_tensor_contiguous_f64, lobpcg_contiguous_f64, lu_factor_contiguous_f64,
[m[32m+    lu_solve_contiguous_f64, lu_solve_mixed_refine_contiguous_f64, matrix_exp_contiguous_f64,
[m[32m+    qr_contiguous_f64, svd_contiguous_f64, svd_lowrank_contiguous_f64, svdvals_contiguous_f64,
[m     symmetric_rank2k_lower_update_f64,
 };
 
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/benches/linalg_bench.rs:391:
         c.bench_function(&format!("cholesky_solve_f64_{n}x{n}_nrhs"), |bch| {
             bch.iter(|| {
                 black_box(
[31m-                    cholesky_solve_contiguous_f64(&factor, black_box(&id), &id_meta, false).unwrap(),
[m[32m+                    cholesky_solve_contiguous_f64(&factor, black_box(&id), &id_meta, false)
[m[32m+                        .unwrap(),
[m                 )
             })
         });
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:475:
         csc: isize,
     ) {
         unsafe {
[31m-            matrixmultiply::dgemm(
[m[31m-                m, k, n, alpha, a, rsa, csa, b, rsb, csb, beta, c, rsc, csc,
[m[31m-            );
[m[32m+            matrixmultiply::dgemm(m, k, n, alpha, a, rsa, csa, b, rsb, csb, beta, c, rsc, csc);
[m         }
         set_mxcsr_default();
     }
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:505:
         csc: isize,
     ) {
         unsafe {
[31m-            matrixmultiply::sgemm(
[m[31m-                m, k, n, alpha, a, rsa, csa, b, rsb, csb, beta, c, rsc, csc,
[m[31m-            );
[m[32m+            matrixmultiply::sgemm(m, k, n, alpha, a, rsa, csa, b, rsb, csb, beta, c, rsc, csc);
[m         }
         set_mxcsr_default();
     }
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:5123:
         padded, batch, in_ch, pd, ph, pw, kd, kh, kw, od, oh, ow, sd, sh, sw,
     );
     let mut out_flat = vec![0.0f32; flat * out_ch];
[31m-    gemm::sgemm_bt(flat, patch_width, out_ch, &panel, weight_flat, &mut out_flat);
[m[32m+    gemm::sgemm_bt(
[m[32m+        flat,
[m[32m+        patch_width,
[m[32m+        out_ch,
[m[32m+        &panel,
[m[32m+        weight_flat,
[m[32m+        &mut out_flat,
[m[32m+    );
[m     let mut out = vec![0.0f32; batch * out_ch * patch_count];
     out.par_chunks_mut(patch_count)
         .enumerate()
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:8577:
     // and order (forward k ascending, back k descending). Transpose to RHS-outer
     // so each column is contiguous, solve in parallel, transpose back. The
     // transposes are O(n*num_rhs) vs the O(n^2*num_rhs) solve. frankentorch-l9xod-inv.
[31m-    let par_gate =
[m[31m-        num_rhs >= 8 && (n as u128) * (n as u128) * (num_rhs as u128) >= (1u128 << 26);
[m[32m+    let par_gate = num_rhs >= 8 && (n as u128) * (n as u128) * (num_rhs as u128) >= (1u128 << 26);
[m     if par_gate {
         let lu = &factor.lu;
         let mut xt = vec![0.0f64; num_rhs * n];
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:9679:
     // frankentorch-otbok). Bit-exact: each column reproduces the serial sweep's
     // per-element arithmetic and order. Transpose to RHS-outer, solve in parallel,
     // transpose back. frankentorch-chsolve-colpar.
[31m-    let par_gate =
[m[31m-        num_rhs >= 8 && (n as u128) * (n as u128) * (num_rhs as u128) >= (1u128 << 29);
[m[32m+    let par_gate = num_rhs >= 8 && (n as u128) * (n as u128) * (num_rhs as u128) >= (1u128 << 29);
[m     if par_gate {
         let mut xt = vec![0.0f64; num_rhs * n];
         for i in 0..n {
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:12034:
 
     let mut scratch = a.to_vec();
     let (w_bidiag, v) = golub_reinsch_svd_right_only(&mut scratch, m, n)?;
[31m-    if !w_bidiag.iter().all(|value| value.is_finite())
[m[31m-        || !v.iter().all(|value| value.is_finite())
[m[31m-    {
[m[32m+    if !w_bidiag.iter().all(|value| value.is_finite()) || !v.iter().all(|value| value.is_finite()) {
[m         return Ok(None);
     }
     let max_s = w_bidiag.iter().copied().fold(0.0f64, f64::max);
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:12067:
         }
     }
 
[31m-    Ok(Some(SvdResult { u, s, vh, m, n, k: n }))
[m[32m+    Ok(Some(SvdResult {
[m[32m+        u,
[m[32m+        s,
[m[32m+        vh,
[m[32m+        m,
[m[32m+        n,
[m[32m+        k: n,
[m[32m+    }))
[m }
 
 /// SVD for tall/square matrices (m >= n) via Golub-Reinsch bidiagonalization.
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:16162:
         // NOT the cross-test pollution source (it stays default through a GEMM +
         // transcendental sweep); the guard keeps it that way.
         super::set_mxcsr_default();
[31m-        assert_eq!(super::read_mxcsr(), 0x1f80, "MXCSR not at default after reset");
[m[32m+        assert_eq!(
[m[32m+            super::read_mxcsr(),
[m[32m+            0x1f80,
[m[32m+            "MXCSR not at default after reset"
[m[32m+        );
[m 
         // Only the CONTROL bits matter for determinism: FTZ (bit 15), DAZ
         // (bit 6) and the rounding mode (bits 13-14). The low 6 bits are sticky
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:16217:
         let bias: Vec<f64> = (0..out_ch).map(|i| (i as f64) * 0.05 - 0.1).collect();
 
         let got = super::conv2d_forward_f64(
[31m-            &padded, &weight, Some(&bias), batch, in_ch, ph, pw, kh, kw, oh, ow, sh, sw, out_ch,
[m[32m+            &padded,
[m[32m+            &weight,
[m[32m+            Some(&bias),
[m[32m+            batch,
[m[32m+            in_ch,
[m[32m+            ph,
[m[32m+            pw,
[m[32m+            kh,
[m[32m+            kw,
[m[32m+            oh,
[m[32m+            ow,
[m[32m+            sh,
[m[32m+            sw,
[m[32m+            out_ch,
[m         );
 
         let patch_count = oh * ow;
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:16224:
         let flat = batch * patch_count;
         let patch_width = in_ch * kh * kw;
[31m-        let panel =
[m[31m-            super::conv2d_im2col_f64(&padded, batch, in_ch, ph, pw, kh, kw, oh, ow, sh, sw);
[m[32m+        let panel = super::conv2d_im2col_f64(&padded, batch, in_ch, ph, pw, kh, kw, oh, ow, sh, sw);
[m         let mut out_flat = vec![0.0f64; flat * out_ch];
         super::gemm::dgemm_bt(flat, patch_width, out_ch, &panel, &weight, &mut out_flat);
         let mut want = vec![0.0f64; batch * out_ch * patch_count];
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:16239:
         }
         assert_eq!(got.len(), want.len());
         for (i, (g, w)) in got.iter().zip(want.iter()).enumerate() {
[31m-            assert_eq!(g.to_bits(), w.to_bits(), "1x1 conv mismatch @{i}: {g} vs {w}");
[m[32m+            assert_eq!(
[m[32m+                g.to_bits(),
[m[32m+                w.to_bits(),
[m[32m+                "1x1 conv mismatch @{i}: {g} vs {w}"
[m[32m+            );
[m         }
 
         // f32 mirror: same bit-exact check against im2col_f32 + sgemm_bt.
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:16247:
         let weight32: Vec<f32> = weight.iter().map(|&v| v as f32).collect();
         let bias32: Vec<f32> = bias.iter().map(|&v| v as f32).collect();
         let got32 = super::conv2d_forward_f32(
[31m-            &padded32, &weight32, Some(&bias32), batch, in_ch, ph, pw, kh, kw, oh, ow, sh, sw,
[m[32m+            &padded32,
[m[32m+            &weight32,
[m[32m+            Some(&bias32),
[m[32m+            batch,
[m[32m+            in_ch,
[m[32m+            ph,
[m[32m+            pw,
[m[32m+            kh,
[m[32m+            kw,
[m[32m+            oh,
[m[32m+            ow,
[m[32m+            sh,
[m[32m+            sw,
[m             out_ch,
         );
         let panel32 =
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:16254:
             super::conv2d_im2col_f32(&padded32, batch, in_ch, ph, pw, kh, kw, oh, ow, sh, sw);
         let mut out_flat32 = vec![0.0f32; flat * out_ch];
[31m-        super::gemm::sgemm_bt(flat, patch_width, out_ch, &panel32, &weight32, &mut out_flat32);
[m[32m+        super::gemm::sgemm_bt(
[m[32m+            flat,
[m[32m+            patch_width,
[m[32m+            out_ch,
[m[32m+            &panel32,
[m[32m+            &weight32,
[m[32m+            &mut out_flat32,
[m[32m+        );
[m         let mut want32 = vec![0.0f32; batch * out_ch * patch_count];
         for n in 0..batch {
             for oc in 0..out_ch {
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:16265:
             }
         }
         for (i, (g, w)) in got32.iter().zip(want32.iter()).enumerate() {
[31m-            assert_eq!(g.to_bits(), w.to_bits(), "1x1 conv f32 mismatch @{i}: {g} vs {w}");
[m[32m+            assert_eq!(
[m[32m+                g.to_bits(),
[m[32m+                w.to_bits(),
[m[32m+                "1x1 conv f32 mismatch @{i}: {g} vs {w}"
[m[32m+            );
[m         }
     }
 
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:16277:
         let (kh, kw, sh, sw) = (1usize, 1usize, 1usize, 1usize);
         let (oh, ow) = (ph, pw);
         let n_in = batch * in_ch * ph * pw;
[31m-        let padded: Vec<f64> = (0..n_in).map(|i| ((i as f64 * 0.013).sin()) * 0.5).collect();
[m[31m-        let weight: Vec<f64> = (0..out_ch * in_ch).map(|i| ((i as f64 * 0.017).cos()) * 0.3).collect();
[m[32m+        let padded: Vec<f64> = (0..n_in)
[m[32m+            .map(|i| ((i as f64 * 0.013).sin()) * 0.5)
[m[32m+            .collect();
[m[32m+        let weight: Vec<f64> = (0..out_ch * in_ch)
[m[32m+            .map(|i| ((i as f64 * 0.017).cos()) * 0.3)
[m[32m+            .collect();
[m         let bias: Vec<f64> = (0..out_ch).map(|i| (i as f64) * 0.001).collect();
         let iters = 60u32;
 
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:16286:
         let t0 = Instant::now();
         for _ in 0..iters {
             let o = super::conv2d_forward_f64(
[31m-                &padded, &weight, Some(&bias), batch, in_ch, ph, pw, kh, kw, oh, ow, sh, sw, out_ch,
[m[32m+                &padded,
[m[32m+                &weight,
[m[32m+                Some(&bias),
[m[32m+                batch,
[m[32m+                in_ch,
[m[32m+                ph,
[m[32m+                pw,
[m[32m+                kh,
[m[32m+                kw,
[m[32m+                oh,
[m[32m+                ow,
[m[32m+                sh,
[m[32m+                sw,
[m[32m+                out_ch,
[m             );
             std::hint::black_box(&o);
         }
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:20206:
             }
             assert_eq!(got.len(), want.len());
             for (i, (&g, &w)) in got.iter().zip(want.iter()).enumerate() {
[31m-                assert_eq!(g.to_bits(), w.to_bits(), "shape={shape:?} dim={dim} idx={i}");
[m[32m+                assert_eq!(
[m[32m+                    g.to_bits(),
[m[32m+                    w.to_bits(),
[m[32m+                    "shape={shape:?} dim={dim} idx={i}"
[m[32m+                );
[m             }
 
             // f32 path too.
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:20226:
                 }
             }
             for (i, (&g, &w)) in got32.iter().zip(want32.iter()).enumerate() {
[31m-                assert_eq!(g.to_bits(), w.to_bits(), "f32 shape={shape:?} dim={dim} idx={i}");
[m[32m+                assert_eq!(
[m[32m+                    g.to_bits(),
[m[32m+                    w.to_bits(),
[m[32m+                    "f32 shape={shape:?} dim={dim} idx={i}"
[m[32m+                );
[m             }
         }
     }
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:21739:
             a[i * n + i] = (i as f64) + 1.0;
         }
         let meta = TensorMeta::from_shape(vec![n, n], DType::F64, Device::Cpu);
[31m-        let one_thread = rayon::ThreadPoolBuilder::new().num_threads(1).build().unwrap();
[m[31m-        let four_threads = rayon::ThreadPoolBuilder::new().num_threads(4).build().unwrap();
[m[32m+        let one_thread = rayon::ThreadPoolBuilder::new()
[m[32m+            .num_threads(1)
[m[32m+            .build()
[m[32m+            .unwrap();
[m[32m+        let four_threads = rayon::ThreadPoolBuilder::new()
[m[32m+            .num_threads(4)
[m[32m+            .build()
[m[32m+            .unwrap();
[m 
         let serial = one_thread.install(|| super::eig_contiguous_f64(&a, &meta).unwrap());
         let parallel = four_threads.install(|| super::eig_contiguous_f64(&a, &meta).unwrap());
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:21747:
         assert_eq!(serial.n, parallel.n);
[31m-        for (i, (&s, &p)) in serial.eigenvalues.iter().zip(&parallel.eigenvalues).enumerate() {
[m[32m+        for (i, (&s, &p)) in serial
[m[32m+            .eigenvalues
[m[32m+            .iter()
[m[32m+            .zip(&parallel.eigenvalues)
[m[32m+            .enumerate()
[m[32m+        {
[m             assert_eq!(
                 s.to_bits(),
                 p.to_bits(),
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:21752:
                 "eigenvalue bit mismatch at interleaved slot {i}: {s} vs {p}"
             );
         }
[31m-        for (i, (&s, &p)) in serial.eigenvectors.iter().zip(&parallel.eigenvectors).enumerate() {
[m[32m+        for (i, (&s, &p)) in serial
[m[32m+            .eigenvectors
[m[32m+            .iter()
[m[32m+            .zip(&parallel.eigenvectors)
[m[32m+            .enumerate()
[m[32m+        {
[m             assert_eq!(
                 s.to_bits(),
                 p.to_bits(),
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:22697:
         for idx in 0..n * kk {
             max_diff = max_diff.max((x[idx] - x_true[idx]).abs());
         }
[31m-        assert!(max_diff < 1e-9, "lstsq_qr solution diverges: max_diff={max_diff}");
[m[32m+        assert!(
[m[32m+            max_diff < 1e-9,
[m[32m+            "lstsq_qr solution diverges: max_diff={max_diff}"
[m[32m+        );
[m 
         // Rank-deficient (duplicate column) -> defer to SVD via None.
         let mut a_rd = a.clone();
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:22706:
         }
         let deferred = super::lstsq_qr_contiguous_f64(&a_rd, &a_meta, &b, &b_meta)
             .expect("lstsq_qr should not error on rank-deficient input");
[31m-        assert!(deferred.is_none(), "rank-deficient input must defer to SVD (None)");
[m[32m+        assert!(
[m[32m+            deferred.is_none(),
[m[32m+            "rank-deficient input must defer to SVD (None)"
[m[32m+        );
[m 
         // Underdetermined (m < n) -> defer.
         let a_wide_meta = TensorMeta::from_shape(vec![2usize, 3usize], DType::F64, Device::Cpu);
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:22731:
         assert_eq!(tall_pinv.len(), 6);
         let tall_expected = [0.5, 0.0, 0.0, 0.0, 0.25, 0.0];
         for (got, want) in tall_pinv.iter().zip(tall_expected) {
[31m-            assert!((got - want).abs() < 1e-12, "tall pinv mismatch {got} vs {want}");
[m[32m+            assert!(
[m[32m+                (got - want).abs() < 1e-12,
[m[32m+                "tall pinv mismatch {got} vs {want}"
[m[32m+            );
[m         }
 
         let wide = vec![2.0, 0.0, 0.0, 0.0, 4.0, 0.0];
Diff in /data/projects/frankentorch/crates/ft-kernel-cpu/src/lib.rs:22742:
         assert_eq!(wide_pinv.len(), 6);
         let wide_expected = [0.5, 0.0, 0.0, 0.25, 0.0, 0.0];
         for (got, want) in wide_pinv.iter().zip(wide_expected) {
[31m-            assert!((got - want).abs() < 1e-12, "wide pinv mismatch {got} vs {want}");
[m[32m+            assert!(
[m[32m+                (got - want).abs() < 1e-12,
[m[32m+                "wide pinv mismatch {got} vs {want}"
[m[32m+            );
[m         }
     }
 
